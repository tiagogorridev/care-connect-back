# Application
spring.application.name=care-connect-api

# Server
server.port=8080

# Database MySQL
spring.datasource.url=jdbc:mysql://localhost:3306/care-connect-db
spring.datasource.username=root
spring.datasource.password=
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA/Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect
spring.jpa.properties.hibernate.format_sql=true

# Encoding
spring.datasource.hikari.connection-test-query=SELECT 1
server.servlet.encoding.charset=UTF-8
server.servlet.encoding.enabled=true

# ========================
# JWT CONFIG
# ========================
jwt.secret=troque-por-um-segredo-bem-grande-e-aleatorio
jwt.issuer=careconnect-api
jwt.access-exp-ms=3600000    
jwt.refresh-exp-ms=1209600000   
